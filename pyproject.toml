[project]
name = "rag"
version = "0.1.0"
description = "Local retrieval-augmented generation pipeline built with LangChain and Ollama"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
